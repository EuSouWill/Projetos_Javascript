let cores_string = "vermelho,azul,branco,cinza,roxo";

let cores_array = cores_string.split(",");

console.log(cores_array);

// o caractere usado para "quebrar" a string em partes menores aparece separando
// os itens dentro da propria string. nesse caso é a virgula.
